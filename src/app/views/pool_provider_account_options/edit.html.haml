= render :partial => 'layouts/admin_nav'

%header.page-header
  %h1.no-icon= _('Edit Weight')

%section.content-section
  .content
    = conductor_form_for(@pool_provider_account_option, :method => :put,
                         :url => pool_provider_selection_provider_account_option_path(@pool, @pool_provider_account_option)) do |f|
      = render :partial => "form", :locals => { :form => f }
      = f.button :form_buttons, :cancel => pool_provider_selection_provider_account_options_path(@pool)
