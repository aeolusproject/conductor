%h3
  Properties for
  = @account.name
%p
  %label
    Running instances quota:
  = @account.quota.maximum_running_instances or 'unlimited'
%p
  %label
    Account ID:
  = @account.credentials_hash['account_number']
= link_to "Edit", edit_provider_account_path(@account), { :class => 'button' }
= link_to "Test", provider_account_path(@account, {:test_account => true}), { :class => 'button' }
