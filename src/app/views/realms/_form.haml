= form.error_messages
%fieldset.clear
  = form.label :name,'Realm Name :'
  = form.text_field :name, :title => 'realm_name', :value => @realm.name, :class => "clear grid_4 alpha"
%fieldset.clear
  = form.submit "Save",  :class => "submit formbutton"
