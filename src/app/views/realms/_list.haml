- if @realm_names.size == 0
  %h1 There are no realms to display
- else
  %table
    %thead
      %tr
        %th{:scope => "col"} Name
    %tbody
      - @realm_names.each {|realm_name|
      %tr
        %td= realm_name
      - }
