require 'costengine'

# FIXME: If we decide to keep the costs as a optional component we get back to
# this. Otherwise we probably move the cost* methods from the mixins to the
# apropriate models
#CostEngine.infect_models
