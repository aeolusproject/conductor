!!! XML
%providers
  - @providers.each do |p|
    %provider{:id => p.id, :href => api_provider_url(p)}
      %name= p.name
      %url= p.url
      %provider_type= p.provider_type.deltacloud_driver
      %deltacloud_provider= p.deltacloud_provider
