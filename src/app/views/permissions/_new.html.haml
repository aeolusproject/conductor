%header.page-header
  %h1{:class => controller.controller_name} Grant Access
  #obj_actions.button-group
    Return to
    = link_to @return_text, @return_path, :class => 'button primary', :id => 'new_pool_button'
  .corner &nbsp;

%section.content-section.pools
  = @summary_text
  %header
    %h2 Users

  .content
    = render :partial => 'form'
