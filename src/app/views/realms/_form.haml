- if @realm.errors.any?
  - @realm.errors.full_messages.each do |err|
    = err
%fieldset
  = form.label :name, 'Realm Name'
  = form.text_field :name, :title => 'realm_name', :value => @realm.name, :class => 'em long'
%fieldset.options
  = form.submit "Save",  :class => "submit button"
