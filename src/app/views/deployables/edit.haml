%h2 Editing Deployable: #{@deployable.name}

- form_for @deployable, :url => deployable_path(@deployable), :html => { :method => :put } do |f|
  = render :partial => "form", :locals => { :form => f }
