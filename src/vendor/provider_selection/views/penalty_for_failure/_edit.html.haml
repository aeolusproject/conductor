= form.input :penalty_percentage, :label => _('Percentage penalty'),
             :note => _('Percentage penalty for each failure')
= form.input :time_period_minutes, :label => _('Time interval'),
             :note => _('Time period over which the failures are aggregated (in minutes)')
= form.input :failure_count_hard_limit, :label => _('Hard limit for failures'),
             :note => _('The number of failures before the Provider Account should not be used')
